# ConvertKit (Kit) — server-only, never prefix with NEXT_PUBLIC_
KIT_API_KEY=your_kit_api_key_here
KIT_FORM_ID=your_kit_form_id_here

# Used as the referrer value when adding a subscriber to the Kit form
NEXT_PUBLIC_SITE_URL=https://your-quiz-domain.com
